import EmptyState from '../../../components/ui/EmptyState'

export default function ReglasTab() {
  return (
    <EmptyState
      title="Reglas de coloreo"
      description="Aquí podrás ajustar los umbrales numéricos que pintan las celdas de la matriz de cobertura (blanco / verde / amarillo / rojo). Próximamente."
    />
  )
}
