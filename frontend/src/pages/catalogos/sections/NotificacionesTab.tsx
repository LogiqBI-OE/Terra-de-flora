import EmptyState from '../../../components/ui/EmptyState'

export default function NotificacionesTab() {
  return (
    <EmptyState
      title="Notificaciones"
      description="Define reglas de alerta automáticas por email (ej: 'avisar si un SKU cae en rojo en las próximas N semanas'). Próximamente."
    />
  )
}
